{
  "name": "statowrel-app",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:models": "turbo build --filter=@statowrel/models",
    "build:admin": "turbo build --filter=@statowrel/admin",
    "build:functions": "turbo build --filter=@statowrel/functions",
    "build:dev:ios": "npm run build:models && npm run build:dev:ios --workspace=@statowrel/app",
    "build:dev:android": "npm run build:models && npm run build:dev:android --workspace=@statowrel/app",
    "build:preview:ios": "npm run build:models && npm run build:preview:ios --workspace=@statowrel/app",
    "build:preview:android": "npm run build:models && npm run build:preview:android --workspace=@statowrel/app",
    "build:prod:ios": "npm run build:models && npm run build:prod:ios --workspace=@statowrel/app",
    "build:prod:android": "npm run build:models && npm run build:prod:android --workspace=@statowrel/app",
    "submit:prod:ios": "npm run submit:prod:ios --workspace=@statowrel/app",
    "submit:prod:android": "npm run submit:prod:android --workspace=@statowrel/app",
    "submit:prod": "npm run submit:prod:ios && npm run submit:prod:android",
    "dev": "turbo dev",
    "dev:app": "turbo dev --filter=@statowrel/app",
    "dev:admin": "turbo dev --filter=@statowrel/admin",
    "dev:functions": "turbo dev --filter=@statowrel/functions",
    "set-admin": "node apps/functions/scripts/set-admin.mjs",
    "seed-questions": "npm run build:models && node apps/functions/scripts/seed-questions.mjs",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "deploy:functions": "firebase use default && npm run deploy --workspace=@statowrel/functions",
    "deploy:functions:production": "firebase use production && npm run deploy --workspace=@statowrel/functions && firebase use default",
    "deploy:firestore": "firebase use default && npm run deploy --workspace=@statowrel/firestore-config",
    "deploy:firestore:production": "firebase use production && npm run deploy --workspace=@statowrel/firestore-config && firebase use default",
    "deploy:admin": "firebase use default && npm run deploy --workspace=@statowrel/admin",
    "deploy:admin:production": "firebase use production && npm run deploy --workspace=@statowrel/admin && firebase use default"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.12",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
