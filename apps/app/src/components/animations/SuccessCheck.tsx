import { Animation, type AnimationPresetProps } from './Animation';

/** The bare check with its burst — the lighter twin of `SuccessCircle`. */
export const SuccessCheck = (props: AnimationPresetProps) => <Animation {...props} name="success-check" />;
