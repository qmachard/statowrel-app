import { Animation, type AnimationPresetProps } from './Animation';

/** The shooting star, comet trail behind it — the celebratory twin of the success pair. */
export const Star = (props: AnimationPresetProps) => <Animation {...props} name="star" />;
