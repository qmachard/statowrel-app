import { Animation, type AnimationPresetProps } from './Animation';

/** The filled circle that closes on a check — the answer-confirmed animation. */
export const SuccessCircle = (props: AnimationPresetProps) => <Animation {...props} name="success-circle" />;
