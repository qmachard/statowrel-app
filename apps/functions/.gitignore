dist
node_modules
.firebase-emulator
*.local
firebase-debug.log
firebase-debug.*.log

# Firebase Functions env files — they hold the service account private key.
# Only the template is tracked.
.env
.env.*
!.env.example
firebase-export-*

# Throwaway bundle an ops script builds to run the TypeScript in src/ (scripts/lib/load-src.mjs).
.script-bundle

# Where `npm run render-instagram-card` drops the JPEGs it renders.
.instagram-preview
